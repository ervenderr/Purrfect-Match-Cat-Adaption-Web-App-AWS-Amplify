const originData = [];
for (let i = 0; i < 20; i++) {
    originData.push({
        key: i.toString(),
        name: `Edward ${i}`,
        address: 'New York No. 1 Lake Park',
        cat: "Orens",
        status: 'Pending',
    });
}

export default originData;